--- shadow-4.0.4.1/lib/getdef.c
+++ shadow-4.0.4.1/lib/getdef.c
@@ -347,8 +347,6 @@ def_load(void)
 	 * Open the configuration definitions file.
 	 */
 	if ((fp = fopen(def_fname, "r")) == NULL) {
-		SYSLOG((LOG_CRIT, "cannot open login definitions %s [%m]",
-			def_fname));
 		return;
 	}
 
