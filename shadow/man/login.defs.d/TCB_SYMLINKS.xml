<!--
   Based on changes in the old man page by Solar Designer <solar@owl.openwall.com>
   and Rafal Wojtczuk <nergal@owl.openwall.com>
-->
<varlistentry>
  <term><option>TCB_SYMLINKS</option> (boolean)</term>
  <listitem>
    <para>
      If <replaceable>yes</replaceable>
      the location of the user tcb directory to be created will not be
      automatically set to <filename>/etc/tcb/user</filename>,
      but will be computed depending on the UID of the user, according to the
      following algorithm:
      <programlisting>

if (UID is less than 1000)
    use /etc/tcb/user;
else if (UID is less than 1000000) {
    use /etc/tcb/:kilosK/user, where kilos is calculated as UID / 1000;
    make symlink /etc/tcb/user to the directory;
} else {
    use /etc/tcb/:megasM/:kilosK/user, where megas is calculated as UID / 1000000 and kilos is calculated as (UID - megas *
        1000000) / 1000;
    make symlink /etc/tcb/user to the directory;
}

      </programlisting>
    </para>
  </listitem>
</varlistentry>
